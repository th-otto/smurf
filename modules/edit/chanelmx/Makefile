top_srcdir=../../..
include $(top_srcdir)/config.mak

LIBS += -lm
LDFLAGS += -nostartfiles

MODULES = \
	$(DISTDIR)/modules/edit/chanelmx.sem \
	$(empty)

all:: $(MODULES)

$(DISTDIR)/modules/edit/chanelmx.sem: ../edstart.o chanelmx.o
	$(CC) $(CFLAGS) $(LDFLAGS) -o $@ $^ $(LIBS)

clean::
	$(RM) *.o ../edstart.o $(MODULES)
