top_srcdir=../../..
include $(top_srcdir)/config.mak

LIBS += -lm
LDFLAGS += -nostartfiles

MODULES = \
	$(DISTDIR)/modules/edit/text.sem \
	$(empty)

all:: $(MODULES)

$(DISTDIR)/modules/edit/text.sem: ../edstart.o text.o cookie.o fontsel.o
	$(CC) $(CFLAGS) $(LDFLAGS) -o $@ $^ $(LIBS)

clean::
	$(RM) *.o ../edstart.o $(MODULES)
